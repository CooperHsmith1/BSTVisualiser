package main.bst;

public class BST {
    TreeNode root;

    public void insert(int value)
    {
        if (root == null){
            root = new TreeNode(value);
        }
        else if (value < root.value)
        {


        }
        else if (value > root.value)
        {

        }
    }
}
